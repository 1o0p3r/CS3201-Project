#pragma once
#include <string>
#include <vector>
#include <sstream>
using namespace std;

class Util {
public:
	static string insertBrackets(string input);
	vector<string> Util::splitLine(string s, char delim);
};